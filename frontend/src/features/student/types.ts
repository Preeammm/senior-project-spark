export type Me = {
  studentId: string;
  name: string;
  surname: string;
  faculty: string;
  minor: string;
  year: number;

  avatarUrl?: string | null;

  githubUrl?: string;
  linkedinUrl?: string;
};
